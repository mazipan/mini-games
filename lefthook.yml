commit-msg:
  commands:
    commitlint:
      run: bunx --no -- commitlint --edit {1}

pre-commit:
  commands:
    prettier:
      glob: "*.{html,njk,css,js,json,md}"
      run: bunx prettier --write {staged_files}
      stage_fixed: true
